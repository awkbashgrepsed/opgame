# Rendering notes

The first visible-world renderer uses Win32 OpenGL compatibility-profile calls for simple prototype primitives. The `gl` crate remains available for modern entry points such as clearing, depth testing, and viewport setup.
