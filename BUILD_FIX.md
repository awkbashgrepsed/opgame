# Build fix

The renderer requires the `winapi` `winuser` feature and uses shared `windef::HGLRC` for the OpenGL context handle. The compatibility OpenGL functions are linked from `opengl32.dll` because they are not generated by the current `gl` crate bindings.
